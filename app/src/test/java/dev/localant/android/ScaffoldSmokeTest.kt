package dev.localant.android

import org.junit.Assert.assertTrue
import org.junit.Test

class ScaffoldSmokeTest {
    @Test
    fun appPackageExists() {
        assertTrue("MainActivity class must exist", true)
    }
}
